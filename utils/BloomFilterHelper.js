// ** THIS FUNCTION WORKS FINE WITH THE BASIC PATTERN **
// const checkBlacklistedURL = (postText) => {
//   const blacklistedPattern = /LOLOBALOLO/i; // This is how we define regex.
//   return blacklistedPattern.test(postText); //Returns true if there is a match between the pattern and the text.
// };

// module.exports = checkBlacklistedURL;


